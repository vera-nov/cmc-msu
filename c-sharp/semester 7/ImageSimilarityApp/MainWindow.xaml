<Window x:Class="ImageSimilarityApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Image Similarity (ArcFace)"
        Height="600" Width="800"
        Background="#1E1E1E"
        FontFamily="Segoe UI"
        FontSize="13"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <SolidColorBrush x:Key="AppBackground" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="CardBackground" Color="#252526"/>
        <SolidColorBrush x:Key="CardBorder" Color="#3F3F46"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="AccentBrushHover" Color="#66BB6A"/>
        <SolidColorBrush x:Key="TextMain" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="TextMuted" Color="#B0B0B0"/>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="Background" Value="#333337"/>
            <Setter Property="BorderBrush" Value="#4A4A4A"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="6"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrushHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2C2C2F"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                                <Setter Property="Cursor" Value="Arrow"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0"
                                    Background="{StaticResource CardBackground}"
                                    Padding="4,0"
                                    Margin="8,0,0,0">
                                <ContentPresenter ContentSource="Header"
                                                  VerticalAlignment="Center"/>
                            </Border>

                            <Border Grid.Row="1"
                                    CornerRadius="8"
                                    Background="{StaticResource CardBackground}"
                                    BorderBrush="{StaticResource CardBorder}"
                                    BorderThickness="1"
                                    Padding="5">
                                <ContentPresenter/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Foreground" Value="{StaticResource TextMain}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="border"
                                CornerRadius="4"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#303040"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3F3F5A"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <!-- ListBox с БД -->
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Верхние кнопки -->
        <StackPanel Grid.Row="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,5">
            <Button Grid.Row="0" Grid.Column="0"
                    Name="BtnLoadLeft" Click="BtnLoadLeft_Click">
                Загрузить левое изображение
            </Button>

            <Button Grid.Row="0" Grid.Column="1"
                    Name="BtnLoadRight" Click="BtnLoadRight_Click">
                Загрузить правое изображение
            </Button>
        </StackPanel>

        <!-- Картинки -->
        <Border Grid.Row="1" Grid.Column="0" Margin="5"
                BorderBrush="{StaticResource CardBorder}"
                BorderThickness="1"
                Background="{StaticResource CardBackground}"
                CornerRadius="8">
            <Image Name="ImgLeft" Stretch="Uniform"/>
        </Border>

        <Border Grid.Row="1" Grid.Column="1" Margin="5"
                BorderBrush="{StaticResource CardBorder}"
                BorderThickness="1"
                Background="{StaticResource CardBackground}"
                CornerRadius="8">
            <Image Name="ImgRight" Stretch="Uniform"/>
        </Border>

        <!-- Пути к изображениям -->
        <TextBlock Grid.Row="2" Grid.Column="0" Margin="5"
                   Name="TxtLeftPath"
                   Foreground="{StaticResource TextMuted}"
                   Text="Левое изображение не загружено"/>

        <TextBlock Grid.Row="2" Grid.Column="1" Margin="5"
                   Name="TxtRightPath"
                   Foreground="{StaticResource TextMuted}"
                   Text="Правое изображение не загружено"/>

        <!-- Sim/Dist -->
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal"
                    Margin="5" HorizontalAlignment="Center">
            <TextBlock Text="Similarity: "
                       Margin="5,0"
                       Foreground="{StaticResource TextMuted}"/>
            <TextBlock Name="TxtSimilarity"
                       Width="100"
                       FontWeight="SemiBold"/>

            <TextBlock Text="  Distance: "
                       Margin="5,0"
                       Foreground="{StaticResource TextMuted}"/>
            <TextBlock Name="TxtDistance"
                       Width="100"
                       FontWeight="SemiBold"/>

            <TextBlock Text="  Status: "
                       Margin="15,0"
                       Foreground="{StaticResource TextMuted}"/>
            <TextBlock Name="TxtStatus"
                       Width="200"/>
        </StackPanel>

        <!-- Clear button -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal"
                    Margin="5" HorizontalAlignment="Center">
            <Button Name="BtnClearDb" Click="BtnClearDb_Click" Margin="5">
                Clear
            </Button>
        </StackPanel>

        <!-- ListBox БД -->
        <GroupBox Grid.Row="5" Grid.ColumnSpan="2" Margin="5"
                  Header="БД">
            <ListBox x:Name="LstResults" Margin="5">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Image1Name}" Margin="0,0,4,0"/>
                            <TextBlock Text=" - " Margin="0,0,4,0"/>
                            <TextBlock Text="{Binding Image2Name}" Margin="0,0,8,0"/>
                            <TextBlock Text="Sim=" Margin="0,0,2,0"
                                       Foreground="{StaticResource TextMuted}"/>
                            <TextBlock Text="{Binding Similarity}" Margin="0,0,8,0"/>
                            <TextBlock Text="Dist=" Margin="0,0,2,0"
                                       Foreground="{StaticResource TextMuted}"/>
                            <TextBlock Text="{Binding Distance}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
    </Grid>
</Window>
